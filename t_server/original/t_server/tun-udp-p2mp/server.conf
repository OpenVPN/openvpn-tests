port 51194
proto udp6
dev tun

# MASTER ONLY
local *
local 127.0.0.1 30000
local 0.0.0.0 30001
local :: 30002
local 0.0.0.0 30002 tcp
local tserver-rocky-9-amd64.tserver.site 30003
# MASTER ONLY

ca /root/openvpn-test-server/keys/ca.crt
cert /root/openvpn-test-server/keys/server.crt
key /root/openvpn-test-server/keys/server.key
#dh /root/openvpn-test-server/keys/dh.pem

# Configure server mode and supply a VPN subnet
# for OpenVPN to draw client addresses from.
# The server will take 10.8.0.1 for itself,
# the rest will be made available to clients.
# Each client will be able to reach the server
# on 10.8.0.1. Comment this line out if you are
# ethernet bridging. See the man page for more info.
server 10.204.2.0 255.255.255.0
server-ipv6 fd00:abcd:204:2::/64

# Maintain a record of client <-> virtual IP address
# associations in this file.  If OpenVPN goes down or
# is restarted, reconnecting clients can be assigned
# the same virtual IP address from the pool that was
# previously assigned.
ifconfig-pool-persist ipp.txt 60

# new default in 2.7 -> this test needs net30 (ipp.txt)
topology net30

# Push routes to the client to allow it
# to reach other private subnets behind
# the server.  Remember that these
# private subnets will also need
# to know to route the OpenVPN client
# address pool (10.8.0.0/255.255.255.0)
# back to the OpenVPN server.
push "route 10.204.0.0 255.255.0.0"
push "route-ipv6 fd00:abcd:204::/48"

# Client-specific settings
client-config-dir ccd

; gert, 14.09.20, for windows client-to-client test
client-to-client

keepalive 10 30

# Enable compression on the VPN link.
# If you enable it here, you must also
# enable it in the client config file.
comp-lzo

# Output a short status file showing
# current connections, truncated
# and rewritten every minute.
status openvpn-status.log

# Set the appropriate level of log
# file verbosity.
#
# 0 is silent, except for fatal errors
# 4 is reasonable for general usage
# 5 and 6 can help to debug connection problems
# 9 is extremely verbose
verb 4

# auto-switch compression depending on client capabilities
script-security 2
client-connect ../client-connect-switchcomp.sh

cipher BF-CBC        # Blowfish (for 2.2/2.3 client compat)

# poor mans NCP (plus real NCP)
# COMPAT: gert, 13.11.21, add BF-CBC
#ncp-ciphers AES-256-GCM:AES-128-GCM:AES-128-CBC:AES-192-CBC:AES-256-CBC:none:DES-EDE3-CBC
ncp-ciphers AES-256-GCM:AES-128-GCM:AES-128-CBC:AES-192-CBC:AES-256-CBC:none:DES-EDE3-CBC:BF-CBC

# COMPAT: gert, 13.11.21 compat with 2.2/2.3 clients
tls-version-min 1.0
# COMPAT, gert, 15.11.21, add fallback cipher (2.3 / --enable small, no OCC)
data-ciphers-fallback BF-CBC

# COMPAT: gert, 22.12.21, OpenSSL 3.0.0 / sha1 & BF-CBC 
tls-cert-profile insecure
providers legacy default

# gert, 10.10.22 - testing client "no user/password, but pushed auth-token"
# call with "--reneg-sec 90" to see if it works or not
#auth-gen-token 300
# MyTokenUser | base64
#push "auth-token-user TXlUb2tlblVzZXI="
# -> neuer Test, 8.3.25 -> in ccd/ mit "override-username"

# ^^^ tut nicht, von Hand basteln vvvv
auth-user-pass-optional
script-security 3
auth-user-pass-verify aupv.sh via-env
client-connect cc-make-token.sh

#auth-user-pass-optional
#auth-user-pass-verify /bin/true via-env
#
#
tun-mtu 1400 1500
#tun-mtu 2000 1500


# test only (refuses all pre-2.6 clients)
#force-tls-key-material-export

tls-verify tlsv.sh
tls-export-cert /var/tmp

# test commit 9317a769 / #880
# 16.03.25 wieder raus, bricht beim restart oft (EADDRINUSE)
# 18.03.25 wieder rein, ist u.u. mehr EEN
management ::1 61194

#explicit-exit-notify 1
